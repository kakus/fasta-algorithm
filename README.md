#mbi-fasta

#How to build
The build tool used here is gulp, so to build this project you will need:

* nodejs
* gulp | `npm install -g gulp`
* `npm install`

And you should be able to work with this project :).
