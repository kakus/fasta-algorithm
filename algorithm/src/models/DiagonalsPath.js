var fasta;
(function (fasta) {

    function DiagonalsPath(diagonals, score) {
        this.diagonals = diagonals;
        this.score = score;
    }

    fasta.DiagonalsPath = DiagonalsPath;
})(fasta = fasta || {});
